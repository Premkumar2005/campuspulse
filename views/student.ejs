<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | CampusPulse</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css" rel="stylesheet">
  <style>
    body { background: #f0f2f5; }

    /* ── Navbar ────────────────────────── */
    .navbar-brand { font-weight: 800; font-size: 1.3rem; }

    /* ── Hero Banner ───────────────────── */
    .hero-banner {
      background: linear-gradient(135deg, #0d6efd 0%, #6f42c1 100%);
      border-radius: 20px;
      padding: 2rem 2.5rem;
      color: white;
      position: relative;
      overflow: hidden;
    }
    .hero-banner::after {
      content: '🎓';
      position: absolute;
      right: 2rem;
      top: 50%;
      transform: translateY(-50%);
      font-size: 6rem;
      opacity: 0.15;
    }
    .hero-banner h2 { font-weight: 900; font-size: 2rem; }

    /* ── Stat Cards ────────────────────── */
    .stat-card {
      border: none;
      border-radius: 16px;
      padding: 1.4rem;
      color: white;
      box-shadow: 0 4px 15px rgba(0,0,0,0.12);
      transition: transform 0.2s;
      position: relative;
      overflow: hidden;
    }
    .stat-card:hover { transform: translateY(-4px); }
    .stat-card::after {
      content: attr(data-icon);
      position: absolute;
      right: 1rem;
      bottom: 0.5rem;
      font-size: 3.5rem;
      opacity: 0.15;
    }

    .card-blue   { background: linear-gradient(135deg, #0d6efd, #0099ff); }
    .card-green  { background: linear-gradient(135deg, #198754, #20c997); }
    .card-orange { background: linear-gradient(135deg, #fd7e14, #ffc107); }
    .card-purple { background: linear-gradient(135deg, #6f42c1, #d63384); }

    .stat-label { font-size: 0.85rem; opacity: 0.85; margin-bottom: 4px; }
    .stat-value { font-size: 2.2rem; font-weight: 900; line-height: 1; }
    .stat-sub   { font-size: 0.8rem; opacity: 0.75; margin-top: 4px; }

    /* ── Profile Card ──────────────────── */
    .profile-card {
      border: none;
      border-radius: 16px;
      box-shadow: 0 2px 12px rgba(0,0,0,0.08);
    }
    .profile-avatar {
      width: 72px; height: 72px;
      border-radius: 50%;
      background: linear-gradient(135deg, #0d6efd, #6f42c1);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.8rem;
      color: white;
      font-weight: 700;
    }
    .info-row {
      display: flex;
      align-items: center;
      padding: 0.6rem 0;
      border-bottom: 1px solid #f0f0f0;
      font-size: 0.9rem;
    }
    .info-row:last-child { border-bottom: none; }
    .info-label {
      width: 110px;
      color: #6c757d;
      font-weight: 500;
      flex-shrink: 0;
    }

    /* ── Leaderboard ───────────────────── */
    .leaderboard-card {
      border: none;
      border-radius: 16px;
      box-shadow: 0 2px 12px rgba(0,0,0,0.08);
    }
    .lb-row {
      display: flex;
      align-items: center;
      padding: 0.75rem 1rem;
      border-radius: 12px;
      margin-bottom: 6px;
      background: #f8f9fa;
      transition: background 0.2s;
    }
    .lb-row:hover { background: #e9ecef; }
    .lb-row.me {
      background: linear-gradient(135deg, #e8f0fe, #f3e8ff);
      border: 2px solid #0d6efd;
    }
    .lb-rank {
      width: 36px; height: 36px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: 800;
      font-size: 0.9rem;
      flex-shrink: 0;
      margin-right: 12px;
    }
    .rank-1 { background: #ffd700; color: #000; }
    .rank-2 { background: #c0c0c0; color: #000; }
    .rank-3 { background: #cd7f32; color: #fff; }
    .rank-other { background: #e9ecef; color: #495057; }

    .lb-name { font-weight: 600; font-size: 0.95rem; flex-grow: 1; }
    .lb-events {
      font-weight: 700;
      color: #0d6efd;
      font-size: 0.9rem;
    }
    .lb-date { font-size: 0.75rem; color: #adb5bd; }
  </style>
</head>
<body>

<!-- ── Navbar ──────────────────────────────────── -->
<nav class="navbar navbar-dark bg-dark px-4 shadow-sm">
  <div class="d-flex align-items-center">
    <span class="navbar-brand">🚀 CampusPulse</span>
    <span class="badge bg-primary ms-2">Student</span>
  </div>
  <div class="d-flex align-items-center gap-3">
    <span class="text-white-50 small">
      <i class="bi bi-person-circle me-1"></i><%= user.name %>
    </span>
    <a href="/auth/logout" class="btn btn-outline-light btn-sm">
      <i class="bi bi-box-arrow-right me-1"></i>Logout
    </a>
  </div>
</nav>

<div class="container py-4">

  <!-- ── Hero Banner ────────────────────────────── -->
  <div class="hero-banner mb-4">
    <div class="small mb-1" style="opacity:0.75">
      Welcome back 👋
    </div>
    <h2 class="mb-1"><%= profile.name %></h2>
    <div style="opacity:0.8">
      <i class="bi bi-envelope me-1"></i><%= profile.email %>
      &nbsp;·&nbsp;
      <i class="bi bi-calendar me-1"></i>
      Joined <%= new Date(profile.created_at).toLocaleDateString('en-US', { month: 'long', year: 'numeric' }) %>
    </div>
    <!-- My Rank Badge -->
    <div class="mt-3">
      <span style="background:rgba(255,255,255,0.2);
                   border-radius:50px;
                   padding: 6px 16px;
                   font-size:0.9rem;">
        <i class="bi bi-trophy-fill me-1" style="color:#ffd700"></i>
        Your Rank: <strong>#<%= myRank.rank_pos %></strong>
        among all students
      </span>
    </div>
  </div>

  <!-- ── Stat Cards ──────────────────────────────── -->
  <div class="row g-3 mb-4">

    <div class="col-6 col-md-3">
      <div class="stat-card card-blue" data-icon="⚡">
        <div class="stat-label">Total Events</div>
        <div class="stat-value"><%= stats.total_events %></div>
        <div class="stat-sub">all time activity</div>
      </div>
    </div>

    <div class="col-6 col-md-3">
      <div class="stat-card card-green" data-icon="📅">
        <div class="stat-label">Today</div>
        <div class="stat-value"><%= todayCount %></div>
        <div class="stat-sub">events today</div>
      </div>
    </div>

    <div class="col-6 col-md-3">
      <div class="stat-card card-orange" data-icon="📊">
        <div class="stat-label">This Week</div>
        <div class="stat-value"><%= weekCount %></div>
        <div class="stat-sub">last 7 days</div>
      </div>
    </div>

    <div class="col-6 col-md-3">
      <div class="stat-card card-purple" data-icon="🏆">
        <div class="stat-label">Top Action</div>
        <div class="stat-value" style="font-size:1.3rem;margin-top:4px;">
          <%= topFeature.event_type.replace('_',' ') %>
        </div>
        <div class="stat-sub"><%= topFeature.cnt %> times</div>
      </div>
    </div>

  </div>
  <!-- ── END STAT CARDS ──────────────────────────── -->


  <!-- ── Profile + Leaderboard Row ──────────────── -->
  <div class="row g-3">

    <!-- Profile Card -->
    <div class="col-md-4">
      <div class="card profile-card h-100">
        <div class="card-body p-4">

          <h6 class="fw-bold mb-4">
            <i class="bi bi-person-badge me-2 text-primary"></i>
            My Profile
          </h6>

          <!-- Avatar + Name -->
          <div class="d-flex align-items-center mb-4">
            <div class="profile-avatar me-3">
              <%= profile.name.charAt(0).toUpperCase() %>
            </div>
            <div>
              <div class="fw-bold fs-5"><%= profile.name %></div>
              <span class="badge bg-primary"><%= profile.role %></span>
            </div>
          </div>

          <!-- Info Rows -->
          <div class="info-row">
            <div class="info-label">
              <i class="bi bi-envelope me-1 text-muted"></i>Email
            </div>
            <div class="text-truncate small"><%= profile.email %></div>
          </div>

          <div class="info-row">
            <div class="info-label">
              <i class="bi bi-shield me-1 text-muted"></i>Role
            </div>
            <div>
              <span class="badge bg-primary"><%= profile.role %></span>
            </div>
          </div>

          <div class="info-row">
            <div class="info-label">
              <i class="bi bi-calendar me-1 text-muted"></i>Joined
            </div>
            <div class="small">
              <%= new Date(profile.created_at).toLocaleDateString('en-US',{ day:'numeric', month:'short', year:'numeric' }) %>
            </div>
          </div>

          <div class="info-row">
            <div class="info-label">
              <i class="bi bi-clock me-1 text-muted"></i>Last Active
            </div>
            <div class="small">
              <% if (stats.last_active) { %>
                <%= new Date(stats.last_active).toLocaleDateString('en-US',{ day:'numeric', month:'short', year:'numeric' }) %>
              <% } else { %>
                No activity yet
              <% } %>
            </div>
          </div>

          <div class="info-row">
            <div class="info-label">
              <i class="bi bi-trophy me-1 text-muted"></i>Rank
            </div>
            <div>
              <span class="badge bg-warning text-dark">
                #<%= myRank.rank_pos %>
              </span>
            </div>
          </div>

        </div>
      </div>
    </div>

    <!-- Leaderboard -->
    <div class="col-md-8">
      <div class="card leaderboard-card h-100">
        <div class="card-body p-4">

          <h6 class="fw-bold mb-4">
            <i class="bi bi-trophy-fill me-2 text-warning"></i>
            Student Leaderboard — Top 10
          </h6>

          <% leaderboard.forEach((student, index) => {
               const rank = index + 1;
               const isMe = student.name === profile.name;
          %>
          <div class="lb-row <%= isMe ? 'me' : '' %>">

            <!-- Rank Badge -->
            <div class="lb-rank
              <%= rank === 1 ? 'rank-1' :
                  rank === 2 ? 'rank-2' :
                  rank === 3 ? 'rank-3' : 'rank-other' %>">
              <% if (rank === 1) { %>🥇
              <% } else if (rank === 2) { %>🥈
              <% } else if (rank === 3) { %>🥉
              <% } else { %><%= rank %><% } %>
            </div>

            <!-- Name -->
            <div>
              <div class="lb-name">
                <%= student.name %>
                <% if (isMe) { %>
                  <span class="badge bg-primary ms-1"
                    style="font-size:0.7rem">You</span>
                <% } %>
              </div>
              <div class="lb-date">
                Last active:
                <%= new Date(student.last_active).toLocaleDateString('en-US',{ month:'short', day:'numeric' }) %>
              </div>
            </div>

            <!-- Events Count -->
            <div class="ms-auto text-end">
              <div class="lb-events">
                <%= student.total_events %>
              </div>
              <div style="font-size:0.72rem;color:#adb5bd">events</div>
            </div>

          </div>
          <% }) %>

          <% if (leaderboard.length === 0) { %>
          <div class="text-center text-muted py-4">
            <i class="bi bi-people fs-2 d-block mb-2"></i>
            No activity yet. Start exploring!
          </div>
          <% } %>

        </div>
      </div>
    </div>

  </div>
  <!-- ── END PROFILE + LEADERBOARD ──────────────── -->

</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>